<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card border-0 shadow-sm">
        <div class="card-body p-5 text-center">
          <div class="mb-4">
            <i class="fas fa-check-circle text-success" style="font-size: 4rem;"></i>
          </div>
          <h2 class="mb-4">Thanh toán thành công</h2>
          <p class="text-muted mb-4">
            Cảm ơn bạn đã tin tưởng dịch vụ khách sạn của chúng tôi! Chi tiết giao dịch của bạn được hiển thị bên dưới.
          </p>

          <div class="payment-details bg-light p-4 rounded mb-4">
            <div class="row mb-3">
              <div class="col-6 text-start">Mã đơn đặt phòng:</div>
              <div class="col-6 text-end fw-bold"><%= payment.booking._id %></div>
            </div>
            <div class="row mb-3">
              <div class="col-6 text-start">Số tiền:</div>
              <div class="col-6 text-end fw-bold">$<%= payment.amount.toFixed(2) %></div>
            </div>
            <div class="row mb-3">
              <div class="col-6 text-start">Ngày thanh toán:</div>
              <div class="col-6 text-end"><%= payment.paymentDate.toLocaleString() %></div>
            </div>
            <div class="row">
              <div class="col-6 text-start">Phương thức thanh toán:</div>
              <div class="col-6 text-end">
                <%= payment.paymentMethod === 'credit-card' ? 'Thẻ tín dụng' :
                    payment.paymentMethod === 'debit-card' ? 'Thẻ ghi nợ' :
                    payment.paymentMethod === 'bank-transfer' ? 'Chuyển khoản' :
                    payment.paymentMethod === 'paypal' ? 'PayPal' : payment.paymentMethod %>
              </div>
            </div>
          </div>

          <a href="/customer/bookings/<%= payment.booking._id %>" class="btn btn-primary">Xem chi tiết đặt phòng</a>
        </div>
      </div>
    </div>
  </div>
</div>

<style>
.payment-details {
  background: rgba(255, 255, 255, 0.9) !important;
  color: #333;
  border-radius: 10px;
}
</style>